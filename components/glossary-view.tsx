'use client'
import { useEffect, useMemo, useState } from 'react'
import { BookOpen, Search } from 'lucide-react'
import { extraApi, type GlossaryTerm } from '@/lib/api-extra'

export function GlossaryView() { const [terms, setTerms] = useState<GlossaryTerm[]>([]); const [query,setQuery]=useState(''); const [error,setError]=useState(false); useEffect(()=>{extraApi.glossary().then(setTerms).catch(()=>setError(true))},[]); const filtered=useMemo(()=>terms.filter((term)=>`${term.term} ${term.category} ${term.definition}`.toLowerCase().includes(query.toLowerCase())),[terms,query]); return <main className="mx-auto max-w-5xl space-y-8"><div><p className="mb-2 text-sm font-bold text-[#ff7900]">RÉFÉRENCE</p><h2 className="text-3xl font-bold tracking-tight">Glossaire</h2><p className="mt-2 text-sm leading-6 text-black/50">Les indicateurs et notions métier utilisés par Dispatching Intelligent.</p></div><div className="relative max-w-xl"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-black/35"/><input value={query} onChange={(event)=>setQuery(event.target.value)} placeholder="Rechercher un terme" className="w-full rounded-xl border border-black/10 bg-white py-3 pl-10 pr-4 text-sm outline-none focus:border-[#ff7900]"/></div>{error&&<div className="rounded-2xl border border-red-200 bg-red-50 p-4 text-sm text-red-800">Le glossaire n&apos;est pas disponible pour le moment.</div>}{!error&&!filtered.length&&<div className="rounded-2xl border border-dashed border-black/15 bg-white p-10 text-center text-sm text-black/45">Aucun terme trouvé.</div>}<div className="grid gap-4 md:grid-cols-2">{filtered.map((term,index)=><article key={`${term.term}-${term.category}-${index}`} className="rounded-2xl border border-black/5 bg-white p-5 shadow-sm"><div className="flex items-start gap-3"><span className="grid size-9 shrink-0 place-items-center rounded-xl bg-[#fff1e6] text-[#ff7900]"><BookOpen className="size-4"/></span><div><div className="flex flex-wrap items-center gap-2"><h3 className="font-bold">{term.term}</h3><span className="rounded-full bg-black/5 px-2 py-1 text-[10px] font-semibold uppercase tracking-wide text-black/50">{term.category}</span></div><p className="mt-3 text-sm leading-6 text-black/65">{term.definition}</p>{term.example&&<p className="mt-3 rounded-lg bg-[#fafafa] p-3 text-xs leading-5 text-black/50"><strong className="text-black/70">Exemple : </strong>{term.example}</p>}</div></div></article>)}</div></main> }
