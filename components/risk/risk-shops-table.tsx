'use client'
import { useState } from 'react'; import { RiskShop, RegionRisk } from '@/lib/api-risk'; import { formatNumber } from '@/lib/api'
export function RiskShopsTable({rows,onSort}:{rows:RiskShop[];onSort:(sort:string)=>void}){const [sort,setSort]=useState('deficit');function change(v:string){setSort(v);onSort(v)}return <section className="rounded-2xl border bg-white shadow-sm"><div className="border-b p-5"><h2 className="font-semibold">Boutiques prioritaires</h2><p className="mt-1 text-sm text-muted-foreground">L&apos;exposition sature autour de 95 pour la plupart des boutiques ; le déficit distingue réellement les priorités.</p></div><div className="overflow-x-auto"><table className="w-full min-w-[850px] text-sm"><thead className="bg-muted/50 text-left text-xs uppercase text-muted-foreground"><tr>{['Boutique','Gouvernorat','Exposition','Demandé','Alloué','Manquant','Niveau d’action'].map((h,i)=><th key={h} className="px-4 py-3">{i===2?<button onClick={()=>change(sort==='urgency'?'deficit':'urgency')}>{h} ↕</button>:i===5?<button onClick={()=>change(sort==='deficit'?'urgency':'deficit')}>{h} ↕</button>:h}</th>)}</tr></thead><tbody>{rows.map(r=><tr key={r.shop_id} className="border-t"><td className="px-4 py-3 font-medium">{r.shop_name}<span className="block text-xs text-muted-foreground">{r.shop_id}</span></td><td className="px-4 py-3">{r.governorate}</td><td className="px-4 py-3">{r.urgency_score.toFixed(1)}<span className="ml-1 text-xs text-muted-foreground">({r.risk_level})</span></td><td className="px-4 py-3">{formatNumber(r.units_requested)}</td><td className="px-4 py-3">{formatNumber(r.units_allocated)}</td><td className="px-4 py-3 font-semibold text-red-600">{formatNumber(r.units_unserved)}</td><td className="px-4 py-3">{r.action_level}</td></tr>)}</tbody></table></div></section>}
export function RegionTable({rows}:{rows:RegionRisk[]}){return <section className="rounded-2xl border bg-white shadow-sm"><div className="border-b p-5"><h2 className="font-semibold">Synthèse régionale</h2></div><div className="overflow-x-auto"><table className="w-full min-w-[780px] text-sm"><thead className="bg-muted/50 text-left text-xs uppercase text-muted-foreground"><tr>{['Région','Boutiques','Demande menacée','Unités manquantes','Couverture du besoin','Boutiques sans solution'].map(h=><th key={h} className="px-4 py-3">{h}</th>)}</tr></thead><tbody>{[...rows].sort((a,b)=>b.units_unserved-a.units_unserved).map(r=><tr key={r.region} className="border-t"><td className="px-4 py-3 font-medium">{r.region}</td><td className="px-4 py-3">{r.shops}</td><td className="px-4 py-3">{formatNumber(r.demand_7d_at_risk,1)}</td><td className="px-4 py-3">{formatNumber(r.units_unserved)}</td><td className="px-4 py-3">{r.coverage_of_need.toFixed(1)}%</td><td className="px-4 py-3">{r.shops_non_servi}</td></tr>)}</tbody></table></div></section>}
