'use client'
import { AllocationKpis } from '@/lib/api-allocation'
import { formatNumber } from '@/lib/api'
export function AllocationKpiCards({ data }: { data: AllocationKpis }) { const cards = [{ label:'Taux de service du dépôt', value:`${data.depot_service_rate.toFixed(1)}%`, note:`${formatNumber(data.total_allocated)} / ${formatNumber(data.total_requested)} unités` }, { label:'Boutiques servies', value:formatNumber(data.shops_fully_served), note:`${data.shops_partially_served} partielles · ${data.shops_deferred} reportées` }, { label:'Ruptures résolues', value:formatNumber(data.stockouts_resolved), note:'besoins couverts' }, { label:'Retraits de surstock', value:formatNumber(data.withdrawal_lines), note:'lignes de retrait' }]; return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(card=><div key={card.label} className="rounded-2xl border bg-white p-5 shadow-sm"><p className="text-sm text-muted-foreground">{card.label}</p><p className="mt-3 text-3xl font-bold tracking-tight">{card.value}</p><p className="mt-2 text-xs text-muted-foreground">{card.note}</p></div>)}</div> }
