'use client'
import { AllocationProduct } from '@/lib/api-allocation'; import { formatNumber } from '@/lib/api'
export function AllocationProductTable({ rows }: { rows: AllocationProduct[] }) { const sorted=[...rows].sort((a,b)=>b.total_requested-a.total_requested); return <section className="rounded-2xl border bg-white shadow-sm"><div className="border-b p-5"><h2 className="font-semibold">Allocation par produit</h2><p className="mt-1 text-sm text-muted-foreground">Comparaison entre le besoin brut et la capacité réelle du dépôt.</p></div><div className="overflow-x-auto"><table className="w-full min-w-[900px] text-sm"><thead className="bg-muted/50 text-left text-xs uppercase text-muted-foreground"><tr>{['Produit','Dépôt disponible','Demandé','Alloué','Taux de service','Servi','Partiel','Reporté','Ruptures résolues'].map(h=><th key={h} className="px-4 py-3 font-medium">{h}</th>)}</tr></thead><tbody>{sorted.map(r=><tr key={r.product_id} className={`border-t ${r.service_rate<50?'border-l-4 border-l-red-500 bg-red-50/30':''}`}><td className="px-4 py-4 font-medium">{r.product_id}</td><td className="px-4 py-4">{formatNumber(r.depot_available)}</td><td className="px-4 py-4">{formatNumber(r.total_requested)}</td><td className="px-4 py-4">{formatNumber(r.total_allocated)}</td><td className="px-4 py-4"><div className="flex items-center gap-2"><div className="h-2 w-24 rounded-full bg-muted"><div className="h-full rounded-full bg-primary" style={{width:`${Math.min(r.service_rate,100)}%`}}/></div><span>{r.service_rate.toFixed(1)}%</span></div></td><td className="px-4 py-4">{r.n_servi}</td><td className="px-4 py-4">{r.n_partiel}</td><td className="px-4 py-4">{r.n_reporte}</td><td className="px-4 py-4">{r.n_ruptures_resolues}</td></tr>)}</tbody></table></div></section> }
