import type { OrderKpis } from '@/lib/api-orders'
import { fmt } from '@/lib/api-orders'
export function OrderKpiCards({data}:{data:OrderKpis}){const cap=Math.min(data.cap_usage_rate,100);return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><div className="rounded-2xl bg-black p-5 text-white"><p className="text-xs text-white/60">Mode</p><div className="mt-2 flex items-center gap-2"><span className="text-2xl font-bold">{data.mode}</span><span className={`size-2 rounded-full ${data.enabled?'bg-emerald-400':'bg-white/30'}`}/></div><p className={`mt-2 text-xs ${data.enabled?'text-emerald-300':'text-white/50'}`}>{data.enabled?'automatisation active':'automatisation coupée'}</p></div><div className="rounded-2xl border border-orange-100 bg-white p-5"><p className="text-xs text-black/50">En attente</p><p className="mt-2 text-3xl font-bold text-[#ff7900]">{fmt(data.propose)}</p></div><div className="rounded-2xl border bg-white p-5"><p className="text-xs text-black/50">Émis</p><p className="mt-2 text-3xl font-bold">{fmt(data.emis)}</p><p className="mt-1 text-xs text-black/45">{fmt(data.valide)} validés à émettre</p></div><div className="rounded-2xl border bg-white p-5"><div className="flex justify-between text-xs"><span>Plafond du jour</span><b>{fmt(data.cap_usage_rate,1)} %</b></div><div className="mt-3 h-2 overflow-hidden rounded-full bg-black/10"><div className={`h-full ${data.cap_usage_rate>90?'bg-red-500':'bg-[#ff7900]'}`} style={{width:`${cap}%`}}/></div><p className="mt-2 text-xs text-black/45">{fmt(data.units_emitted_today)} / {fmt(data.daily_unit_cap)} unités</p></div></div>}
