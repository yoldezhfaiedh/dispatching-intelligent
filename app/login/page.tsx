'use client'
import { FormEvent, useState } from 'react'
import { Eye, EyeOff, ArrowRight } from 'lucide-react'
import { useRouter } from 'next/navigation'
import { useAuth } from '@/lib/auth'
export default function LoginPage() { const router = useRouter(); const { login } = useAuth(); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [show,setShow]=useState(false); const [error,setError]=useState(''); const submit=(event: FormEvent)=>{event.preventDefault(); setError(''); if(login(email,password)) router.push('/dashboard'); else setError('Identifiants invalides. Vérifiez votre adresse email et votre mot de passe.')} ; return <main className="grid min-h-screen lg:grid-cols-2"><section className="flex min-h-[260px] flex-col justify-between bg-[#050505] p-8 text-white lg:min-h-screen lg:p-16"><div className="flex items-center gap-3"><span className="size-3 rounded-full bg-[#ff7900]"/><span className="font-bold tracking-tight">Dispatching Intelligent</span></div><div className="max-w-md"><h1 className="text-4xl font-bold tracking-tight lg:text-6xl">Pilotez vos stocks avec précision.</h1><p className="mt-5 text-lg leading-7 text-white/55">Une vue claire pour transformer chaque signal en action terrain.</p></div><p className="text-xs text-white/40">Orange Tunisie · Opérations retail</p></section><section className="flex items-center justify-center bg-white p-8"><form onSubmit={submit} className="w-full max-w-md"><p className="text-sm font-bold uppercase tracking-widest text-[#ff7900]">Bienvenue</p><h2 className="mt-3 text-3xl font-bold">Se connecter</h2><p className="mt-2 text-sm text-black/50">Accédez à votre espace de pilotage.</p><div className="mt-8 flex flex-col gap-5"><label className="flex flex-col gap-2 text-sm font-semibold">Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" required placeholder="admin@orange.tn" className="rounded-xl border px-4 py-3 font-normal outline-none focus:border-[#ff7900]"/></label><label className="flex flex-col gap-2 text-sm font-semibold">Mot de passe<div className="flex rounded-xl border focus-within:border-[#ff7900]"><input value={password} onChange={e=>setPassword(e.target.value)} type={show?'text':'password'} required placeholder="••••••••" className="min-w-0 flex-1 rounded-xl px-4 py-3 outline-none font-normal"/><button type="button" onClick={()=>setShow(!show)} aria-label={show?'Masquer le mot de passe':'Afficher le mot de passe'} className="px-4 text-black/45">{show?<EyeOff className="size-4"/>:<Eye className="size-4"/>}</button></div></label>{error&&<p role="alert" className="text-sm text-red-600">{error}</p>}<button className="flex items-center justify-center gap-2 rounded-xl bg-[#ff7900] px-4 py-3.5 font-bold text-white hover:bg-[#e96c00]">Se connecter <ArrowRight className="size-4"/></button></div></form></section></main> }
